import classes from "./UserSummary.module.css";

const MealsSummary = () => {
  return (
    <section className={classes.summary}>
      <p>
        Tap on the Menu to view our exotic meals option. If you are new here,
        click on the register button.
      </p>
    </section>
  );
};

export default MealsSummary;
